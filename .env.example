# Public production origin, without a trailing slash.
SITE_ORIGIN=https://lingo.app

# Create an API key and verify a sending domain in Resend. Set all values only in the host environment.
RESEND_API_KEY=re_xxxxxxxxxxxxxxxxx
RESEND_FROM_EMAIL=Lingo <updates@lingo.app>
RESEND_TO_EMAIL=founder@lingo.app
